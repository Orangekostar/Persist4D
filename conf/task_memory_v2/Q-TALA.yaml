# @package _global_
defaults:
  - common
  - _self_

general:
  experiment_name: Q-TALA

model:
  task_memory_enabled: true

task_memory_training:
  variant: Q-TALA
  state_enabled: true
  matcher_mode: tala
  window_mode: local_pair
