# @package _global_
defaults:
  - common
  - _self_

general:
  experiment_name: M3-BASE-CONT

model:
  task_memory_enabled: true
  task_visual_enabled: false

task_memory_training:
  variant: M3-BASE-CONT
  state_enabled: true
  visual_enabled: false
  matcher_mode: tala
  window_mode: local_pair
  optimizer_updates: 1500
  scheduler_total_updates: 1500
