# @package data
train_dataset:
  _target_: datasets.semseg.SemanticSegmentationDataset
  dataset_name: "rio200"
  data_dir: data/processed/rio200
  image_augmentations_path: conf/augmentation/albumentations_aug.yaml
  volume_augmentations_path: conf/augmentation/volumentations_aug.yaml
  label_db_filepath: data/processed/rio200/label_database.yaml
  change_label_db_filepath: data/processed/rio200/change_label_database.yaml
  color_mean_std: data/processed/rio200/color_mean_std.yaml
  
  mode: ${data.train_mode}
  ignore_label: ${data.ignore_label}
  num_labels: ${data.num_labels}
  num_changes: ${data.num_changes}
  add_raw_coordinates: ${data.add_raw_coordinates}
  add_colors: ${data.add_colors}
  add_normals: ${data.add_normals}
  add_instance: ${data.add_instance}
  # different augs experiments

  filter_out_classes: [0, 2]
  label_offset: 2
  temporal_window: ${data.temporal_window}

validation_dataset:
  _target_: datasets.semseg.SemanticSegmentationDataset
  dataset_name: "rio200"
  data_dir: data/processed/rio200
  image_augmentations_path: null
  volume_augmentations_path: null
  label_db_filepath: data/processed/rio200/label_database.yaml
  change_label_db_filepath: data/processed/rio200/change_label_database.yaml
  color_mean_std: data/processed/rio200/color_mean_std.yaml
  
  mode: ${data.validation_mode}
  ignore_label: ${data.ignore_label}
  num_labels: ${data.num_labels}
  num_changes: ${data.num_changes}
  add_raw_coordinates: ${data.add_raw_coordinates}
  add_colors: ${data.add_colors}
  add_normals: ${data.add_normals}
  add_instance: ${data.add_instance}

  filter_out_classes: [0, 2]
  label_offset: 2
  temporal_window: ${data.temporal_window}

test_dataset:
  _target_: datasets.semseg.SemanticSegmentationDataset
  dataset_name: "rio200"
  data_dir: data/processed/rio200
  image_augmentations_path: null
  volume_augmentations_path: null
  label_db_filepath: data/processed/rio200/label_database.yaml
  change_label_db_filepath: data/processed/rio200/change_label_database.yaml
  color_mean_std: data/processed/rio200/color_mean_std.yaml
  
  mode: ${data.test_mode}
  ignore_label: ${data.ignore_label}
  num_labels: ${data.num_labels}
  num_changes: ${data.num_changes}
  add_raw_coordinates: ${data.add_raw_coordinates}
  add_colors: ${data.add_colors}
  add_normals: ${data.add_normals}
  add_instance: ${data.add_instance}

  filter_out_classes: [0, 2]
  label_offset: 2
  temporal_window: ${data.temporal_window}
