# Persist4D Gain V2：交付前审核记录

**日期：2026-09-22**  
**审核对象：`Persist4D_Codex_Gain_V2_FINAL.md`**  
**主文件 SHA256：`9b92e302d0af269614e86a76591cd324374f83fe64b097aa4c2ffdb330c3b688`**

## 1. 审核结论与实际范围

已完成源码/产物对照、执行指令的语义审核，以及 **31/31 项静态/数值策略检查**。没有运行服务器实验，没有训练新模型，没有执行这些V2新CLI，也没有替Codex向用户仓库推送V2开发代码。这里的检查通过不是仓库单元测试通过，更不是完整训练或涨点验证。

主指令是唯一执行规范；EVIDENCE补充文件仅解释来源，不能覆盖主指令的阶段、门槛和发布顺序。新增CLI、input_mode schema与调度器是明确的开发任务，而不是假称仓库现在已经支持。

## 2. 审核时处理的实质问题

| 问题 | 最终处理 | 主文件位置 |
|---|---|---|
| 按V1先完成全部感知，再启动修复器，故障会阻塞全部增益路线 | R1-refiner只依赖R1/该任务数据/映射校验，不依赖P锁定 | §8、§14 |
| 新live路径仍可能因旧dev cache解析要求而停止 | live资产与legacy-import资产分开解析 | §2.1、§5 |
| 把V2当作又获得192 GPU-hours | 累计扣除V1已记11.803828054742961，名义余额180.19617194525705，运行时再核账 | §4 |
| 低LR偷偷续接高LR optimizer造成实验解释错误 | H350原调度恢复；L从R1新optimizer；分开recipe与目录 | §3.4、§6 |
| 当前C0回退被误当成“已经证明LR太高” | 只声明动机，采用两档LR×两种损失配对，不以C0是否涨点关闭所有路线 | §1、§6 |
| 无新机制晋级时，可能漏掉真实有用的C0继续训练结果 | 若C0自身有明确开发收益，可只延长一个C0成为CONTINUATION_ONLY候选 | §7.2 |
| NEW_ONLY与PAIR使用不同候选范围，会混淆证据贡献 | 共用路由、配对资格、候选、初始化和抽样，只替换old数值信息 | §8.1 |
| NEW_ONLY仍用历史路由，却被称为完全无历史系统 | 精确写为相同支持范围下不使用old soft/外观信息的控制 | §8.1 |
| 只改修复训练端，eval/profile/bundle却恢复原OLD_NEW行为 | input_mode与parent身份进入schema，所有loader同步支持 | §2.1、§8.4 |
| 零残差验证走shortcut，掩盖bool/heatmap差异 | 必须走真实materialize路径验证 | §8.7 |
| ±2残差无法翻转高置信错误，但被当成通用修复器 | 加一次可改性/互补诊断；本轮不临时放大残差搜索 | §8.6 |
| 换P后直接迁移R1-head，造成训练—推理特征变化 | 只有两条机制分别有信号且预算允许时，在同TRAIN清单上为P重新生成并成对训练 | §9.2 |
| 关联publisher与refiner publisher不同，强行叠加改变配对语义 | 12配置只作为R1独立候选；V2只允许P+匹配训练refiner组合 | §9 |
| FH暂缺再次关闭所有已可运行训练/组件选择 | 尝试完整CAL/SEL FH；FH单支线失败不阻其他任务，其比较保持null | §5.2、§14 |
| 组合排名step含义和门槛浮点边界含糊 | 定义tie_update与1e-6容差；正式全T胜出仍delta>1e-6 | §10.1 |
| 跨模式/多头共享可变archive导致评价串扰 | 每头与每checkpoint独立publisher/accumulator，共享只读上游特征 | §8.8 |
| 旧E1的27.89%被误称当前live关联错误率 | 保留历史缓存来源；必要时fresh CAL重算一次，禁止当AP上界 | §1、§5.4 |
| 用Release存在或空asset表判断已完整发布 | 必需模型/结果清单实齐且验证，缺包即CODE_ONLY并列清单 | §17 |
| 发布后才上传回执，在immutable release下可能失败 | draft期上传必需文件/预发布回执，发布后最终receipt外置 | §17.3 |

## 3. 静态检查说明

自动检查覆盖：来源索引闭合、代码块成对、固定版本和分支、预算加总、历史CAL差值、恢复点、资产依赖、抽象任务图无环、修复模式控制、父模型变化、有限关联范围、C0-only分支、门槛的四种数值示例、缺覆盖返回null、严格胜出边界、测试上限与发布流程。

四个策略示例使用人工构造的差值向量，验证的是文字规则是否有确定结论，不是模型性能：

| 示例 | 规则结果 |
|---|---|
| NEW每T +0.3pp、PAIR每T +0.5pp | 选择PAIR，且满足相对NEW的额外贡献门槛 |
| NEW每T +0.4pp、PAIR每T +0.45pp | 历史额外收益不足，保留更简单NEW |
| NEW无增益、PAIR负增益 | KEEP_PARENT |
| NEW每T +0.29pp、PAIR每T +0.31pp | PAIR虽越过parent基础门槛，但未满足对NEW的额外门槛，KEEP_PARENT |

检查结果原始文件为 `V2_REVIEW_CHECKS.json`。静态检查不代替Codex对新增实现的目标测试、真实forward和完整评价。

## 4. 仍需要Codex实际确认的内容

当前NFS/本地副本可读性、全部输入文件和checkpoint是否仍存在、可用空闲GPU、预算日志是否有新增未记占用、实际stmetrics版本和元数据、Git/Release写权限，以及新代码的运行行为，都需在执行环境确认。

原生FH的smoke成功不等于CAL/SEL/PB完整覆盖。0.5pp平均/1pp长时只是目标，不能保证。若外部阻塞使某条支线不可运行，指令已经规定独立后继、部分交付状态和可恢复记录；不要求编造完成。

## 5. 最终交付

主文件包含完整执行顺序、条件分支、训练/选择/回退、源代码绑定、数据与预算规则、真实确认、有限测试和实际GitHub发布要求。交给Codex执行时以主文件全文为准，不要仅给摘要。
